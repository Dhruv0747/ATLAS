# Project ATLAS installed-hardware switches.
# Copy to ~/.config/project-atlas/hardware.env on the Jetson.
# Set to 1 only after the three ultrasonic sensors and Arduino bridge are
# physically installed and validated. LiDAR remains the primary navigator.
ATLAS_ULTRASONIC_ENABLED=0
