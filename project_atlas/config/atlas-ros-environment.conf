# Keep ROS 2 DDS traffic inside the Jetson. Remote dashboards connect through
# authenticated TCP/WebSocket services (Foxglove, web dashboard, Visual Cloud)
# and do not require DDS multicast on Wi-Fi, USB Ethernet, cellular, or
# Tailscale interfaces.
ROS_LOCALHOST_ONLY=1
