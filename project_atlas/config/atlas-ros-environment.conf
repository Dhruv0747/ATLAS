# Keep every ATLAS user service and maintenance shell in the same ROS graph.
# The former value of 1 split selectively restarted systemd services from
# normal-shell processes and produced false hardware OFFLINE indications.
ROS_LOCALHOST_ONLY=0
