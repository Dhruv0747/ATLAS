[Unit]
Description=Rover Joystick Input (joy_node)

[Service]
Type=simple
ExecStart=/bin/bash -c 'source /opt/ros/jazzy/setup.bash && ros2 run joy joy_node --ros-args -p dev:=/dev/input/js0 -p deadzone:=0.08 -p autorepeat_rate:=20.0'
Restart=always
RestartSec=2

[Install]
WantedBy=default.target
