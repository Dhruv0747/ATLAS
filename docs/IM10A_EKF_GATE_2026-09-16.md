# IM10A EKF eligibility check

## Independent bias validation and candidate deployment

Two sequential 60-second stationary intervals: 602 training samples and 601
held-out validation samples. Sensor-frame bias XYZ rad/s:
[-0.0002441968, 0.0012528359, 0.0040257086]. Applied only in offline validation,
residual integrated yaw was -0.05334 degrees in the second interval. Maximum
validation gap 0.1324 s, no non-increasing timestamps. This supports short-term
bias correction, not temperature stability, compass accuracy or dynamic safety.

Saved im10a_gyro_bias.json and mounting record. Observer now publishes an
additional /im10a/imu/bias_corrected_candidate topic with bias removed and
180-degree mounting yaw applied to angular velocity. No orientation or
acceleration estimate is claimed on this candidate topic. Covariance is
provisional; no TF broadcast or EKF configuration changed. Original raw topic
and displayed device heading are unchanged. Two pure correction/mount tests
passed. Dynamic signs/angle and motor-interference checks remain pending.

## Repeat after mounting confirmation

User confirmed USB toward rover front, component side up, and stationary rover.
Mounting record: ../project_atlas/config/im10a_mounting.yaml (180 degree yaw).
The live TF/EKF configuration remains unchanged.

60.029 seconds: 602 messages, 10.017 Hz, zero reported checksum failures,
zero non-increasing timestamps. Max callback gap 0.367 s and max host-stamp
delivery age 0.271 s. Acceleration magnitude mean 9.7523 m/s².
Mean gyro Z 0.00401155 rad/s (approximately 0.230 degrees/s); integrated
gyro Z +13.7879 degrees and Euler yaw +13.6230 degrees.

Given the user's stationary confirmation, this is an apparent stationary yaw
bias/drift. The mounting yaw correction will NOT remove it. It is not by itself
proof of hardware damage. Need bias calibration followed by a separate held-out
stationary verification and dynamic sign/angle validation. Timing outlier also
requires attention before fusion. EKF eligibility remains NOT PASSED.

## Earlier run

Subscription-only 60.075 s test of deployed /im10a/imu/unvalidated.
No movement, calibration or EKF configuration commands were issued.
Physical stillness was requested but not independently confirmed.

- 602 messages, 10.014 Hz; largest observed gap 0.124 s.
- Maximum ROS receipt age 8.23 ms. This measures driver-to-subscriber delay,
  not sensor acquisition age (driver stamps are host receipt timestamps).
- Zero non-increasing timestamps; reported checksum error count zero.
- Mean acceleration magnitude 9.740 m/s².
- Mean gyro XYZ: 0.001980, 0.000251, 0.004157 rad/s.
- Z gyro standard deviation: 0.001262 rad/s.
- Integrated Z gyro: +14.302 degrees; reported Euler yaw change +14.068 degrees.
- Service restart count zero at inspection.

Communication/timing passed this limited interval, but EKF eligibility is NOT
passed. If physically stationary, this is substantial heading drift/bias; if
the sensor/rover moved, repeat on a motionless mounted sensor before calibration.
Do not automatically subtract this mean without confirmed stillness.

Remaining gates: mounting photo/axis transform, confirmed stationary bias test,
controlled clockwise/counter-clockwise angle comparison, motor interference,
realistic covariance and dropout behavior. Start by evaluating yaw-rate-only
fusion. Do not fuse unvalidated magnetic heading or double-integrated linear
acceleration. No live EKF parameters were changed.
